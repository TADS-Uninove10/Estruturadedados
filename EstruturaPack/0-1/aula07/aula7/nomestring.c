#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
        char tamanho [50]; //tamanho do vetor     
        printf("quantas palavra tem seu nome"); //imprime
        fflush(stdin); //limpa 
        gets (tamanho);//armazena valor do teclato
        tamanho = strlen(str);
        printf("seu nome tem %d ",tamanho);
  system("PAUSE");	
  return 0;
}
