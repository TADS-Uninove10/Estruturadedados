#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
  int a,b,c;
a = b = c = 0;
printf("Digite a quantidade de nos a somar\n");
scanf("&d",&b);
for(a=1; a <=b; a++)
{
      //c=c+ (0*a-1);
      c = c + (2*a-1);
      
      }
printf("a soma dos nos impares e &d\n",c);
  system("PAUSE");	
  return 0;
}







#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[])
{
    int opc=0;
    do
    {
      system("cls");
      prontf("1-Soma dos numeros impares\n");
      prontf("2-Soma dos numeros pares\n");
      printf("3-0 numerofatorial\n");
      printf("0-Sair\n");
      scanf("&d",&opc);
      if(opc == 1 )
      {
            
    
    printf("o total dos impares c; &d",SomaImpares());
    getch();

if (opc == 2)

if (opc == 3)

(opc 1 = 0 );
0;

}